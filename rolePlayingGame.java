import java.util.Scanner

class Main{
  public static main void [] {
    //get input from the user for the size of the grid
    Scanner grid() = new Scanner(System.in);
    private int numRows = grid.nextInt();
    private int numCols = grid.nextInt();
    private String[][] grid;
    
    //print out the grid
    for (int i = 0; i < numRows; i++)
      {
        for (int j = 0; j < numCols; j++)
          {
            System.out.println();
          }
      }
